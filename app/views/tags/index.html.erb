<h1>Tags</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Tickets</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @tags.each do |tag| %>
      <%= render 'tag', tag: tag %>
    <% end %>
  </tbody>
</table>

<p><%= link_to 'New Tag', new_tag_path %></p>
