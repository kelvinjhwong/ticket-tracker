<tr>
  <td>
    <strong><%= link_to project.name, project_path(project) %></strong>
    <br />
    <%= project.description %>
  </td>
  <td><%= project.tickets.count %></td>
  <td><%= project.tickets.count %></td>
</tr>
